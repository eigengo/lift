import Foundation

/**
 * Offline manager maintains a list of offline sessions.
 */
class ExerciseSessionOfflineManager {
    
    func newSession(session: ExerciseSession) -> Void {
        
    }
    
    func sessionsOnDate(date: NSDate) -> [ExerciseSession] {
        return []
    }
    
    func removeSession(id: NSUUID) -> Void {
        
    }
    
}